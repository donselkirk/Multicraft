## See the default craftbukkit.jar.conf for a detailed documentation of the
## format of this file.
[config]
name = Velocity 1.0.5
source = https://ci.velocitypowered.com/job/velocity/192/artifact/proxy/build/libs/velocity-proxy-1.0.5-all.jar
category = Velocity

[encoding]
#encode = system
#decode = system
#fileEncoding = latin-1

[start]
command = "{JAVA}" -Xmx{MAX_MEMORY}M -Xms{START_MEMORY}M -XX:MaxPermSize=128M -Djline.terminal=jline.UnsupportedTerminal -jar "{JAR}" nogui
